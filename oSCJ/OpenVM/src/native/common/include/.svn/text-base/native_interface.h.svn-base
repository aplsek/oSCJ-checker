#ifndef _NATIVE_INTERFACE_H
#define _NATIVE_INTERFACE_H
/*
 * OVM's native interface.  Mostly just a way to report exceptions
 * back to java-land.
 */

#include "jvm_constants/throwables.h"

struct ExceptionInfo {
  const char* message;
  int code;
};
#endif /* _NATIVE_INTERFACE_H */
