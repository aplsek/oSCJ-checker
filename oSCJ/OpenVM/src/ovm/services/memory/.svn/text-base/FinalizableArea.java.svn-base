package ovm.services.memory;

public interface FinalizableArea {

    /**
     * Invokes the finalization methods for all objects currently allocated
     * in this memory area.
     * @return <tt>true</tt> if new objects are created as part
     * of running the current finalizers, and <tt>false</tt> otherwise.
     */
    boolean runFinalizers();
}
