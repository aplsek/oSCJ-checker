package ovm.core.domain;
/**
 * A user-domain is an application domain that sits on top of the 
 * {@link ExecutiveDomain executive domain}. There can be many user
 * domains running within the OVM (though not yet!).
 * <p>This is a marker interface.
 * <p>Why is the marker interface needed?  A user domain is any domain
 * other than the one for which isExecutive() returns true.
 */
public interface UserDomain extends Domain {
}
